<div class="module-name panel">
<!-- TODO Add Visualization to show Percent Accomplished -->
	<% unless @all.nil? %>
		<% @all.each do |mod|%>
			<div class="module-status">
				<%= link_to mod.name, user_course_mod_path(id: mod.id)%>
			</div>
		<% end %>
	<% end %>
</div>
