<div class="yielder">
	<h1>Editing mod</h1>
	<%= simple_form_for @mod, url: {action: 'update', multipart: true } do |f| %>
	  <%= f.error_notification %>
		<%= render 'form', f: f %>
	<% end %>
	<%= link_to 'Show', edit_user_course_mod_path(id: @mod.id) %> |
	<%= link_to 'Back', :back %>
</div>